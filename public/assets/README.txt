Add your own photos here:

- photo.jpg   -> shown in the hero, inside the gradient-ringed frame.
                 Recommended: a portrait-orientation photo (roughly 4:5),
                 at least 720px wide, well-lit, plain-ish background.
                 Until you add this file, a clean gradient monogram
                 ("MC") is shown instead — the layout always looks
                 intentional, never broken.

- resume.pdf  -> linked from the hero's "Download résumé" button.
